import { ShowElementPipe } from './show-element.pipe';

describe('ShowElementPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowElementPipe();
    expect(pipe).toBeTruthy();
  });
});
