import { GetColumnsPipe } from './get-columns.pipe';

describe('GetColumnsPipe', () => {
  it('create an instance', () => {
    const pipe = new GetColumnsPipe();
    expect(pipe).toBeTruthy();
  });
});
