import { GetTagsFromTasksPipe } from './get-tags-from-tasks.pipe';

describe('GetTagsFromTasksPipe', () => {
  it('create an instance', () => {
    const pipe = new GetTagsFromTasksPipe();
    expect(pipe).toBeTruthy();
  });
});
