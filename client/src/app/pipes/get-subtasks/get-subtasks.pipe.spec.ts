import { GetSubtasksPipe } from './get-subtasks.pipe';

describe('GetSubtasksPipe', () => {
  it('create an instance', () => {
    const pipe = new GetSubtasksPipe();
    expect(pipe).toBeTruthy();
  });
});
